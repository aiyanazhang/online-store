class TestData {
	data(test) {
		const temp = input * 10;
		const result = temp;
		return result;
	}

	validateData(data) {
		const const TestData = data !== null;
		return TestData;
	}

	testData(data) {
		const isTmp = data !== null;
		return isTmp;
	}
}